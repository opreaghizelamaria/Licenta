<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_height="wrap_content" android:layout_width="wrap_content" android:panelBackground="@drawable/backgroundgradient">

    <item
        android:id="@+id/profile"
        android:icon="@drawable/profil"
        android:title="Profil" />
    <item android:id="@+id/chat"
        android:title="Hartă și Chat"
        android:icon="@drawable/map"/>
    <item android:id="@+id/home"
        android:title="Acasă"
        android:icon="@drawable/home"/>
    <item
        android:id="@+id/monitorizare"
        android:icon="@drawable/graphic"
        android:title="Monitorizare" />

</menu>